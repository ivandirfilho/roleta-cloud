# requirements.txt - Roleta Cloud

# Validação e serialização
pydantic>=2.0
pydantic-settings>=2.0

# WebSocket server
websockets>=12.0

# Logging estruturado
structlog>=24.0

# Vision OCR (foto_roleta_junho.md): foto -> dados. PaddleOCR-ONNX self-contained,
# CPU, sem binario externo. Opcional em runtime (degradacao graciosa se ausente).
rapidocr-onnxruntime>=1.2,<2

# Observability / debugging (Sprint VF-0)
prometheus-client>=0.20.0
py-spy>=0.4.0

# Process metrics + lock helpers (VF-0 complemento)
psutil>=5.9.0
filelock>=3.13.0

# Database (SQLite é built-in, não precisa de pacote extra)
# Para futuro migration para SurrealDB:
# surrealdb>=0.3.0

# PostgreSQL stack (S2/S3/S5+): Alembic migrations + SQLAlchemy + driver psycopg2.
# Mantemos sqlite3 (built-in) para SqliteDecisionRepository; SQLAlchemy
# é só para Alembic gerenciar schemas PG. Não há acoplamento ainda em runtime.
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0

# Similarity search (opcional, ativar quando volume > 5k decisões)
# lancedb>=0.5.0
# pyarrow>=14.0

# Dev / Test
# pytest>=8.0
# pytest-asyncio>=0.23.0
